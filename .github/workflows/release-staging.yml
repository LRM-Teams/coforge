name: Release staging

# Publishing a local Computer distribution is manual, never automatic on a push to main - see
# docs/release.md's "Local Computer distribution model": a client release leaves a persistent
# artifact set behind, unlike the cloud application track's replace-a-running-service model, so a
# build per merge is waste. Do not add a `push:` trigger here.
on:
  workflow_dispatch:
    inputs:
      version:
        description: >-
          Release version to publish (e.g. 1.2.0 or 0.1.0-rc.1). Leave empty to auto-generate a
          dev version for this run.
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: release-staging
  cancel-in-progress: false

jobs:
  gates:
    name: Repository gates (check, test, build)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false

      - name: Install toolchain
        run: mise install

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check, test, and build
        run: |
          mise run check
          mise run test
          mise run build

  publish:
    name: Publish to the staging release feed
    needs: gates
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false

      - name: Install toolchain
        run: mise install

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate protocol sources
        run: bun run --cwd packages/protocol generate

      - name: Resolve release version
        id: version
        # inputs.version and github.run_number are interpolated into env vars, not directly into
        # the shell script body: a workflow_dispatch string input is caller-controlled, and
        # substituting it inline here would let its literal text be interpreted as shell syntax
        # before isValidReleaseVersion ever gets a chance to reject it in publish.ts.
        env:
          INPUT_VERSION: ${{ inputs.version }}
          RUN_NUMBER: ${{ github.run_number }}
        run: |
          version="$INPUT_VERSION"
          if [ -z "$version" ]; then
            # "-" not "+": a "+" in a version segment can be reinterpreted as a space by some URL
            # middleware once it becomes a feed path segment (docs/release.md's version charset
            # comment makes the same point about a leading "-", for a different reason).
            version="0.0.0-dev.${RUN_NUMBER}-$(git rev-parse --short=12 HEAD)"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Publish release
        env:
          ALIYUN_OSS_ACCESS_KEY_ID: ${{ secrets.ALIYUN_OSS_ACCESS_KEY_ID }}
          ALIYUN_OSS_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_OSS_ACCESS_KEY_SECRET }}
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
          RELEASE_COMMIT: ${{ github.sha }}
        run: |
          bun scripts/release/publish.ts \
            --version "$RELEASE_VERSION" \
            --commit "$RELEASE_COMMIT" \
            --feed-url "https://releases-staging.coforge.cn"

      - name: Record the published version
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
          RELEASE_COMMIT: ${{ github.sha }}
        run: |
          echo "Published version ${RELEASE_VERSION} (commit ${RELEASE_COMMIT}) to the staging feed." \
            >> "$GITHUB_STEP_SUMMARY"
