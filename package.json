{
  "name": "coforge",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:gateway": "cd apps/realtime-gateway && go run ./cmd/gateway",
    "build": "bun run --filter '*' --if-present build && cd apps/realtime-gateway && mkdir -p dist && go build -o dist/realtime-gateway ./cmd/gateway",
    "typecheck": "bun run --filter '*' --if-present typecheck && cd apps/realtime-gateway && go vet ./..."
  }
}
