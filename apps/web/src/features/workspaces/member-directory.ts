/** Shared by the Members page, its Server Function validators and `WorkspaceMembers`. */

/** The Computer filter value for Agents with no Computer in this Workspace. */
export const NO_COMPUTER = "none";
/** Cards per page as the Members page scrolls down. */
export const MEMBER_PAGE_SIZE = 24;
/** The largest page the server returns, whatever a caller asks for. */
export const MEMBER_PAGE_MAX = 50;
