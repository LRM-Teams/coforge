/** How many of the latest Done and of the latest Closed Tasks the Tasks page lists at first, and
 * how many more "Show older" adds each time: finished Tasks only grow, open ones are the work. */
export const FINISHED_TASKS_PAGE = 50;

/** The most finished Tasks of one status a single read lists. */
export const FINISHED_TASKS_MAX = 1000;
