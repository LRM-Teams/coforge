# 周报功能：设计稿对齐清单

日期：2026-09-15
状态：待按条目逐项实现（本文只记录差距，不改产品契约）
对照实现：`apps/web/src/features/records/`、`apps/web/src/server/records/`、[ADR 0009](../adr/0009-workspace-records-weekly-reports.md)、[ADR 0011](../adr/0011-leader-weekly-report-assignment.md)

后续改每一条时，以本文件的 **ID**（`WR-*`）为准，并打开「参照图」列中的 PNG。设计说明以图内底部「交互说明」为准。

## 1. 设计图索引

全部文件在仓库 `front/`。只关心周报，不覆盖笔记。

| 简称 | 文件 | 画板主题 |
| --- | --- | --- |
| **T1** | `front/leader收到的模板_1.png` | Leader 进入记录：默认看最新周报要点；要点正文与 @ 引用跳转 |
| **T2** | `front/leader收到的模板_2.png` | 点击模板芯片：周报模板编辑 + 侧聊自动展开；倒计时；发送 |
| **T3** | `front/leader收到的模板_3.png` | 编辑周报模板：一级/二级标题操作；编辑取消自动发送；设置入口提示 |
| **T4** | `front/leader收到的模板_4.png` | 要点模板 Tab：当前提示词 + 历史版本「重新启用」 |
| **T5** | `front/leader收到的模板_5.png` | 发送确认弹窗：周报模板与要点模板同时发送 |
| **T6** | `front/leader收到的模板_6.png` | 发送成功：toast、按钮置灰、侧聊成功话术；再编辑需先保存再发送 |
| **T7** | `front/leader收到的模板_7.png` | 模板设置：表格管理周报模板 / 要点模板 |
| **T8** | `front/leader收到的模板_8.png` | 点击行打开模板详情弹窗 |
| **T9** | `front/leader收到的模板_9.png` | 创建模板表单（大纲、频率、时间、启用） |
| **T10** | `front/leader收到的模板_10.png` | 发送对象多选与 chip |
| **M1** | `front/Leader 收到员工的周报_1.png` | Leader 打开成员已提交周报：按周分组、未读、Tab 正文 |
| **M2** | `front/Leader 收到员工的周报_2.png` | 收藏 / 分享 / 导出；标题爱心 |
| **M3** | `front/Leader 收到员工的周报_3.png` | 侧聊生成全员周报要点 |
| **M4** | `front/Leader 收到员工的周报_4.png` | 侧聊选择部分成员生成要点 |
| **M5** | `front/Leader 收到员工的周报_5.png` | 要点生成中：列表「正在生成」、内容区空态 |
| **M6** | `front/Leader 收到员工的周报_6.png` | 要点生成完成：正文 + @成员；侧聊完成确认 |

文案层导出 `front/Untitled` 仅作交叉核对，不作为布局依据。

## 2. 当前实现快照（对齐基线）

已有、且与设计部分重合：

- 记录页分「周报 / 笔记」；周报侧栏有搜索、顶部格式芯片、已收藏、我的周报、成员周报（按 ISO 周折叠）、底栏周报统计/周报设置。
- 多条 `applied` 发送配置各自一枚芯片，文案 `{year} W{week} 模板`（多流时追加设置名）；芯片可显示倒计时或「已发送」。
- Leader 从格式文档「发送给成员」创建当周 parent + 成员指派；cron 周期发送；`#general` 通知。
- 成员在「我的周报」填写并提交；提交后出现在 Leader 的 parent 子树。
- 格式文档：内容区「周报模板 / 要点模板」；一级标题为顶栏 Tab，二级为内容卡片；要点模板可编辑提炼提示词与历史。
- 侧聊由助理驱动：Leader 打开格式页或阅读成员周报时自动展开；可生成当周要点（见 [ADR 0014](../adr/0014-weekly-highlight-generation.md)）。
- Catalog 返回 `highlights`（含 year / generating，按周新到旧）；侧栏不再单独列出「周报要点」，要点挂在「成员周报」周节点上（见 ADR 0015）。
- 收藏有 Prisma `WeeklyReportFavorite`、侧栏列表，以及成员周报详情的收藏/取消入口与标题旁爱心。
- `/records` 在周报 Tab、存在要点时，桌面内容区打开最新要点；无要点时为空态。

下文「现状」随切片更新。

## 3. 待对齐条目

优先级只表示建议实现顺序，不是已批准的排期。标了 **可能改契约** 的条目，动手前需先确认是否改 ADR / schema，不要直接写代码。

### A. 信息架构与默认落地

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-01 | 进入「记录」默认落在「周报」Tab，内容区默认展示**最新周报要点**（优先最新成员周节点的要点，否则该周空态；无成员周时回退 catalog highlights） | T1 | **已对齐（IA 更新）**：见 [ADR 0015](../adr/0015-member-week-sidebar-share-export.md) |  |
| WR-02 | 侧栏分组：模板入口芯片 → 已收藏的周报 → 我的周报 → 成员周报（按周；周节点即要点入口） | T1、M1 | **已对齐（IA 更新）**：去掉顶层「周报要点」分组 |  |
| WR-03 | 成员周报按 **Wxx 周报** 周节点分组，其下是该周已提交成员；周节点打开该周要点（无要点则空态+生成）；未提交「未读」仍不做 | T1、M1 | **部分对齐**：周→要点导航已做；**未做**未提交「未读」（WR-37） | 见 [ADR 0015](../adr/0015-member-week-sidebar-share-export.md) |
| WR-04 | 已收藏与要点列表也有「更多…」 | T1、M1 | **已对齐**：默认 3 条，「N 更多 ...」展开；搜索时展示全部 |  |
| WR-05 | 模板芯片文案为 `{year} W{week} 模板`，高亮倒计时；发送后灰显并标「已发送」 | T1、T2、T6、T7 | **已对齐**：芯片为 `{year} W{week} 模板`；多条 applied 时追加 ` · 设置名` 以免撞名。倒计时仍跟现有发送窗，不是 11:00 一小时窗（WR-06）。已发送灰显 +「已发送」。 | 11:00 高亮窗见 WR-06 |

### B. 模板推送时间窗与自动发送

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-06 | 发送日 **11:00** 把模板推给 Leader；侧栏「记录」出现未读圆点；芯片高亮并进入 **1 小时**倒计时 | T1、T2 | **部分对齐**：预览窗 = `sendTime − 1h`（设置里的发送时间，设计稿 12:00 则是 11:00）。芯片倒计时到 `sendTime`；「记录」导航圆点在预览窗内。无独立推送事件/通知 | 见 [ADR 0012](../adr/0012-weekly-report-preview-auto-send.md) |
| WR-07 | **12:00**：若 1 小时内 Leader **未编辑**，自动把模板发给名单；若有编辑，取消自动发送，必须手动发 | T1、T2、T3 | **已对齐**（cron 在 `sendTime` 发；格式正文在预览窗内被改则跳过自动发，当周须手动发） |  |
| WR-08 | 12:00 之后（已发送）模板入口置灰 | T1、T6 | **已对齐**：已发送灰显；倒计时在 `sendTime` 结束。取消自动发后芯片不再高亮倒计时，仍可点开手动发 |  |
| WR-09 | 编辑中：侧聊提示已取消自动推送；内容区发送按钮与模板卡片置灰 | T2、T3、T6 | **已对齐**：预览窗内编辑保存会取消自动发并写入助理话术；未保存时发送钮置灰。侧聊卡片本身不置灰 |  |
| WR-10 | 侧聊发送钮旁显示倒计时；点 X 可关侧聊 | T2 | **已对齐**：格式页侧聊默认展开可关；预览窗内发送钮旁显示倒计时 |  |
| WR-11 | 「取消本周周报」：LRM 组不需要；面向外部客户可保留 | T2、T6 | 无此按钮 | 建议 MVP 不做 |

### C. 周报模板编辑器（Leader 格式文档）

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-12 | 内容区顶部分「周报模板 / 要点模板」两个入口，默认周报模板 | T2、T3、T4 | **已对齐**：格式页两个入口；默认周报模板；要点模板可编提示词与历史 |  |
| WR-13 | 右上角主操作是「发送」；编辑后出现「保存」+ 置灰「发送」，须先保存再发送 | T2、T3、T6 | **已对齐**：格式页（周报大纲 + 要点提示词）仅手搓「保存」；未保存时发送置灰。成员指派与笔记仍可 autosave | 见 [ADR 0014](../adr/0014-weekly-highlight-generation.md) |
| WR-23 | 点「发送」先出确认弹窗：同时发送周报模板和要点模板 | T5 | **已对齐**：确认文案承诺双发；投递仍只建周报指派，提示词供后续提炼（ADR 0013） |  |
| WR-24 | 成功 toast「周报已分享给相关成员」，3s 消失；发送钮与芯片置灰 | T6 | **已对齐**（toast 3s；发送钮与芯片仍由当周已发送态置灰） |  |
| WR-14 | Leader 只编一级标题（顶栏 Tab）和二级标题（内容区）；更深层级由成员在自己周报里加 | T3 | **已对齐**（格式页 Tab=一级，卡片=二级；成员周报编辑器仍可更深） |  |
| WR-15 | 「+ 一级标题」出空白 Tab；hover Tab 出 x 删除；左右箭头切换溢出 Tab | T3 | **已对齐**：新增后立即空白输入；hover 显示删除；溢出时左右箭头 |  |
| WR-16 | hover 二级标题左右出现增/删图标 | T3 | **已对齐**：二级卡片 hover 左侧增、右侧删 |  |
| WR-17 | 识别到编辑时，左下角设置图标弹出提示：细项去「周报设置」改 | T3 | **已对齐**：未保存编辑时底栏设置旁弹出提示，可关闭 |  |

### D. 要点模板与要点正文

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-18 | 要点模板 = Leader 可编辑的提炼提示词 | T4 | **已对齐**：存在于格式文档 `content.highlightPrompt`（见 [ADR 0013](../adr/0013-weekly-highlight-prompt.md)） |  |
| WR-19 | 下方历史提示词：更新时间 +「重新启用」 | T4 | **已对齐**：保存新文案时归档旧文案；「重新启用」填回编辑框 |  |
| WR-20 | 要点正文：一、本周进展 / 二、下周计划；条目下附出处与 @成员；点击跳到该成员周报 | T1、M6 | **已对齐**：生成结果为两段 + `sources`；`@姓名` 打开成员周报 |  |
| WR-21 | 要点页顶栏周序号徽章、完成时间；默认打开最新一份 | T1、M6 | **已对齐**：周序号徽章 + 完成时间；默认打开见 WR-01 |  |

### E. 发送确认、成功态、双模板同发

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-22 | 侧聊由助理驱动 | T2、T3、T6、M3–M6 | **已对齐**：服务端写 `assistant` 评论与卡片；格式页/Leader 读成员周报默认展开（[ADR 0014](../adr/0014-weekly-highlight-generation.md)）。本切片抽取为确定性规则，不是云端 LLM |  |
| WR-23 | 点「发送」先出确认弹窗：同时发送周报模板和要点模板 | T5 | **已对齐**：确认文案承诺双发；投递仍只建周报指派，提示词供后续提炼（ADR 0013） |  |
| WR-24 | 成功 toast「周报已分享给相关成员」，3s 消失；发送钮与芯片置灰 | T6 | **已对齐**（toast 3s；发送钮与芯片仍由当周已发送态置灰） |  |
| WR-25 | 保存后助理询问是否发送 | T6 | **已对齐**：点「保存」且仍可发时，助理贴 `offer-send`；autosave 不问（ADR 0014） |  |

### F. 模板设置

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-26 | 设置页顶栏 Tab：周报模板 / 要点模板；右上「+ 创建模板」 | T7、T9 | **已对齐（要点模板 Tab 为空占位）** | 完整要点模板管理见后续切片 |
| WR-27 | 表格列：模板名称、发送对象、发送频率、发送时间、是否启用、编辑/删除 | T7 | **已对齐**；「是否启用」点击切换启用态（`applied`+`scheduleEnabled`，见 WR-33） |  |
| WR-28 | 点击行打开只读「模板详情」 | T8 | **已对齐** |  |
| WR-29 | 创建/编辑：名称 10 汉字或 20 字母，超限输入框变红 | T9 | **已对齐**（输入中即时校验） |  |
| WR-30 | 大纲：+一级 / 二级；「三级标题以此类推」 | T9 | 对话框支持一/二级 | 与 WR-14 需统一 |
| WR-31 | 发送频率：周 / 月 / 季 / 年 | T9 | 写死 `weekly` | **可能改契约** |
| WR-32 | 发送时间：0–24 点整点 | T9 | **已对齐**：00:00–23:00 整点；服务端拒绝非整点 |  |
| WR-33 | 「是」启用出现芯片；「否」不发送 | T9 | **已对齐**：创建/编辑/表格切换把 `applied` 与 `scheduleEnabled` 写成同一布尔；启用时确保格式文档 | 见 [ADR 0011](../adr/0011-leader-weekly-report-assignment.md) |
| WR-34 | 发送对象：下拉多选、全部成员、chip 可叉掉；展示职务 | T9、T10 | **已对齐**（职务用 Workspace 角色，因无岗位字段） |  |

### G. 成员周报阅读（Leader）

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-35 | 标题 `{姓名} {year} W{week} 工作周报`；分享时间与分享人 | M1 | **已对齐**：标题重算为该格式；顶栏「分享于 / 分享者」用提交时间与模板作者 |  |
| WR-36 | 一级标题为顶栏 Tab | M1、M2 | 已有 `ReportTabsEditor` 只读 | 较接近 |
| WR-37 | 未提交成员在当周列表显示「未读」 | M1 | 未提交的人根本不出现（ADR 0011） | **可能改契约** |
| WR-38 | 更多菜单：收藏、分享、导出；收藏后标题旁爱心 | M2 | **已对齐**：收藏 + 复制链接分享 + Markdown 导出；收藏爱心 |  |
| WR-39 | 侧聊可对成员周报评论 | M2、M3 | **已对齐**：人工评论仍可用；Leader 读成员周报时默认展开助理侧聊 |  |

### H. 从成员周报生成要点

| ID | 要对齐的行为 | 参照图 | 现状 | 备注 |
| --- | --- | --- | --- | --- |
| WR-40 | Leader 在侧聊说「生成本周周报要点」→ 全员 / 选择成员 | M3 | **已对齐**：空线程助理卡片；输入该句会出选择器 |  |
| WR-41 | 选择成员：名单 + 确认 | M4、M5 | **已对齐**：勾选已提交成员后确认生成 |  |
| WR-42 | 生成中：侧栏「正在生成」；内容区空态 | M5 | **已对齐**：`content.generating`；同步抽取时侧栏闪现取决于请求时长 |  |
| WR-43 | 完成后打开要点文档 | M6 | **已对齐**：生成成功后导航到要点 |  |
| WR-44 | 要点条目可 @ 多名成员 | T1、M6 | **已对齐**：条目 `sources` 可多名；点击跳转 | 与 WR-20 一起 |

## 4. 建议切片顺序

1. **侧栏与默认打开**：WR-01、WR-02（先挂上已有 highlights）、WR-04、WR-21 — **已做**
2. **设置页表格化与详情**：WR-26（仅周报模板 Tab）、WR-27、WR-28、WR-29、WR-32、WR-34 — **本切片已做**
3. **发送确认与成功态（仍只发周报指派）**：WR-23 双发文案、WR-24 toast、WR-13 若确认放弃 autosave — **本切片已做（autosave 保留；投递仍只建周报指派）**
4. **格式编辑器与芯片文案**：WR-05、WR-14–WR-17、WR-12 的周报模板一半 — **本切片已做**
5. **收藏读写**：WR-38 的收藏部分 — **本切片已做（分享/导出未做）**
6. **时间窗 / 编辑取消自动发**：WR-06–WR-10 — **本切片已做**
7. **要点模板 + 双发**：WR-12、WR-18、WR-19、WR-23 完整版 — **本切片已做**
8. **助理侧聊与生成要点**：WR-22、WR-40–WR-44、WR-20 — **本切片已做**（确定性抽取，非 LLM）
9. **保存后问发送 + 侧聊倒计时**：WR-25、WR-09、WR-10 — **本切片已做**
10. **成员周侧栏 + 分享导出**：WR-03（仅已提交）、WR-35、WR-38 — **本切片已做**（WR-37 未提交未读仍不做）
11. **格式页纯手搓保存**：WR-13 — **已做**（去掉格式页 autosave / blur 落库；要点提示词同）
12. **启用语义合一**：WR-33 — **本切片已做**（`applied` ≡ `scheduleEnabled` 产品开关）

与 ADR 冲突最大、不宜夹在小 UI 修复里的是：**WR-37、WR-31**。

## 5. 明确不做（除非另开条目）

- 笔记 Tab 的视觉精修
- T2 注明的 LRM「取消本周周报」（WR-11）
- 设计稿里的全局导航视觉，除非「记录」未读圆点（WR-06）需要动 AppShell
- 用 `front/Untitled` 还原间距/色值

## 6. 开工约定

1. 一次只做一个或一组紧密 ID，先补失败测试（领域/serverfn），UI 按 [testing.md](../agents/testing.md) 不写组件单测。
2. 若条目 **可能改契约**，先更新或增补 ADR，再写实现。
3. 改完在本文件对应行把「现状」改成「已对齐」，不要另起一份文档。

## 7. 当前 TODO（2026-09-15）

以下项目来自 `front/` 设计稿与当前实现的逐项对照，按“先不改产品契约、先完成视觉和已有行为对齐”的顺序执行：

- [ ] **契约确认**：确认 WR-31 是否支持周 / 月 / 季 / 年；确认 WR-37 是否展示未提交成员。两项确认前不改 schema、调度或成员可见性契约。
- [ ] **要点详情**：对齐顶部「本周进展 / 下周计划 / 要点总结」Tab、重点内容块、`@成员` 展示、右上更多入口和生成中空态（M5、M6）。
- [ ] **侧聊**：对齐助理消息、模板附件、接收成员头像组、发送状态、成员选择器和底部附件 / 语音 / 图标发送区（T2、T5、T6、M3–M5）。附件上传和语音输入当前仅保留设计位置，未接入行为。
- [ ] **报告头部**：对齐成员头像、周序号徽章、标题、分享时间 / 分享人和收藏入口（M1、M2）。已补成员周报作者头像；周序号、信息分行和收藏入口仍待继续调整。
- [ ] **模板编辑器**：对齐一级 Tab、二级标题栏 / 正文区、hover 增删操作和编辑态层级（T2、T3）。
- [ ] **模板设置**：对齐平铺表格、详情弹窗、创建模板纵向表单、发送对象 chip 和成员多选（T7–T10）。
- [ ] **记录侧栏**：对齐分组顺序、成员周节点、未读呈现及模板芯片状态（T1、M1）。
- [ ] **每个切片验收**：不新增 UI 单元测试；按 `docs/agents/testing.md` 手工检查桌面 / 移动、light / dark、键盘 / Escape / 触屏、滚动和溢出，并记录结果。

### 当前切片

- [x] 要点详情第一步：完成顶部 Tab、内容块层级和生成中空态；未改变要点数据模型、生成流程或服务端契约。
- [ ] 要点详情第二步：补齐更多操作、滚动状态同步和正文中的来源成员呈现。
- [x] 侧聊基础视觉：完成模板附件卡片、成员选择器的头像 / 顶部确认操作、评论输入区的附件 / 语音 / 图标布局；附件上传与语音输入仍未接入行为。
- [x] 报告头部第一步：成员周报头部增加作者头像，保留现有移动返回和更多操作。
- [x] 报告头部第二步：复用 `WeekBadge` 展示报告周期，将成员头像 / 作者名放入标题 meta 行，并提供独立收藏按钮，降低窄屏横向挤压。
- [x] 报告顶部布局：按参考稿拆分为「周期标题 / 操作」「周报模板 / 要点模板」「内容维度」三层，压缩顶部垂直间距并将内容 Tab 改为紧凑选中态。
