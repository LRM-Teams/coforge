{
  "name": "@coforge/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "test": "bun test",
    "generate": "../../node_modules/.bin/buf generate",
    "check": "../../node_modules/.bin/buf lint && ../../node_modules/.bin/buf format --diff --exit-code && oxfmt --check . && oxlint . && tsc --noEmit",
    "pretest": "bun run generate",
    "precheck": "bun run generate"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0",
    "typescript": "7.0.2"
  }
}
