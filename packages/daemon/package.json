{
  "name": "@coforge/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "test": "bun test --max-concurrency=1",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "typecheck": "tsc --noEmit",
    "check": "bun run format:check && bun run lint && bun run typecheck",
    "build": "COFORGE_DAEMON_VERSION=$(bun -p 'require(\"./package.json\").version'); export COFORGE_DAEMON_VERSION; bun build --compile --env=COFORGE_DAEMON_* ../cli/src/cli.ts --outfile ./dist/coforge && bun build --compile --env=COFORGE_DAEMON_* ./index.ts --outfile ./dist/coforge-daemon",
    "pretest": "bun run build"
  },
  "dependencies": {
    "@coforge/agent": "0.1.0",
    "@coforge/cli": "workspace:*",
    "@coforge/protocol": "workspace:*",
    "@logtape/logtape": "2.3.2",
    "@logtape/redaction": "2.3.2",
    "centrifuge": "5.2.1"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0",
    "typescript": "7.0.2"
  }
}
